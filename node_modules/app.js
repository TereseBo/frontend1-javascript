const prompt= require("prompt-sync")()
const path = require('path');
//Nedan följer en switch för att köra övriga .js filer i projektet via absolute path.

console.log('Starta här:')
let app= prompt("Vad vill du starta (var / if / funk / mod?")
switch (app)
{
    case "var":
        const variabler=require("..\\01-variabler\\app.js")
    break;
    case"if":
        const ifswich=require("..\\02-if-switch\\app.js")
            break;
    case "funk":
        const funk=require(`..\\03-funktioner\\app.js`)
    break;
    case "mod":
        console.log("Men du är ju redan här?!")
        const mod=require(`..\\node_modules\\app.js`) //Avslutar programmet istället för att starta igen
    break;
    default:
        console.log(`Du skrev nog fel?`)
        const def=`..\\mapp.js`
        break;
    
}